{
  "name": "customer-assist-poc",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "frontend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n \"API,Frontend\" -c \"green,blue\" \"npm run start:dev -w api\" \"npm run dev -w frontend\""
  },
  "keywords": [],
  "author": "",
  "description": "",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}